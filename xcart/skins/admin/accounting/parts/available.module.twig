{##
 # module
 #}

<a href="{{ this.moduleParam.link.href }}" target="_blank" class="marketplace-search">
    <img src="{{ this.layout.getResourceWebPath(this.moduleParam.img.src) }}" alt="{{ this.moduleParam.img.alt }}">
    <span class="link-wrapper">{{ this.moduleParam.link.name }}</span>
</a>
