{##
 # Attribute groups list table template
 #
 # @ListChild (list="itemsList.attributegroup.header.before", weight="10")
 #}

<div class="alert alert-info note {{ this.getAdditionalPanelStyle() }}">{{ t('Editing attribute groups on this page won\'t affect other product classes which use these groups') }}</div>
