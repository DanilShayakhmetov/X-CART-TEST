{##
 # No attributes
 #}

<div class="{{ this.getBlockStyle() }}">
  <div class="header">
    <h2>{{ t('Product variants') }}</h2>
  </div>
  <div class="content">
    <p>
      <span>{{ t('The product does not have attributes with multiple options') }}</span>
      {{ widget('\\XLite\\View\\Tooltip', text=t('To set product variants you should select attributes with multiple options.'), isImageTag='true', className='help-icon') }}
    </p>
    <br/>
    <p>
      {{ widget('\\XLite\\View\\Button\\Link', style='main-button', label=t('Add attributes'), location=url('product', '', {'product_id': this.product.product_id, 'page': 'attributes'})) }}
    </p>
  </div>
</div>
